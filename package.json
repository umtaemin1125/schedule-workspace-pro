{
  "name": "schedule-workspace-pro",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint:commit": "commitlint --from=HEAD~1 --to=HEAD"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  }
}
